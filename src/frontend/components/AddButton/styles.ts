/* AddButton */

import styled from 'styled-components'
import { FONT_SIZE_1_Px } from '../../constants/numbers'
import { GRAY } from '../../constants/colors'


export const RootContainer = styled.div`
  font-size: ${FONT_SIZE_1_Px};
  font-weight: bold;
  color: ${GRAY};
	margin-block: 10px;
  cursor: pointer;
`
